# Docker

### Important commands
```
docker run -p 3000:3000 <name>


docker build -t <tagname>


docker login
docker images
docker ps
docker kill <id>
docker rmi <id> --force


docker tag <tagname> <reponame>
docker push <reponame>


docker exec -it <id> sh
```